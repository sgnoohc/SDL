#ifndef Algo_h
#define Algo_h

namespace SDL
{
    // Mini-Doublet algorithm enum
    enum MDAlgo
    {
        Default_MDAlgo = 0,
        AllComb_MDAlgo
    };

    // Segment algorithm enum
    enum SGAlgo
    {
        Default_SGAlgo = 0,
        AllComb_SGAlgo
    };

    // Tracklet algorithm enum
    enum TLAlgo
    {
        Default_TLAlgo = 0,
        AllComb_TLAlgo
    };
};

#endif
