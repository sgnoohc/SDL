#ifndef Algo_h
#define Algo_h

namespace SDL
{
    // Mini-Doublet algorithm enum
    enum MDAlgo
    {
        Default_MDAlgo = 1,
        AllComb_MDAlgo = 2
    };

    // Segment algorithm enum
    enum SGAlgo
    {
        Default_SGAlgo = 1,
        AllComb_SGAlgo = 2
    };

    // Tracklet algorithm enum
    enum TLAlgo
    {
        Default_TLAlgo = 1,
        AllComb_TLAlgo = 2
    };
};

#endif
